---
import Layout from '../../layouts/Layout.astro';
import Feed from '../../components/Feed.astro';

const { listId } = Astro.params;
---

<Layout title="Trending">
  <Feed url={`playlist?id=${listId}`} isList={true} />
</Layout>