---
export interface Props {
  text: string;
  route: string;
}

const { text, route } = Astro.props as Props;
---

<a href={route} class="h-10 px-3 rounded-md flex items-center gap-6 select-none hover:bg-zinc-100 dark:hover:bg-zinc-800">
  <slot />
  <span>{ text }</span>
</a>